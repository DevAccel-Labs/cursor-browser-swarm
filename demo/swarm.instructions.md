Use the demo app as a QA-minded engineer. Test visible flows only.

If authentication is needed in a real app, use credential environment variables
such as `SWARM_SECRET_EMAIL` and `SWARM_SECRET_PASSWORD`; do not print secret
values in reports.
